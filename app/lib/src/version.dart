/// The app's version as `pubspec.yaml` states it (without the build
/// number); a test keeps the two in step. Shown on the About card and
/// carried into package names by the workspace tool.
const appVersion = '0.10.0';
