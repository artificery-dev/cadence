# The workspace is pure Dart; the Flutter app in app/ resolves on its own
# (it needs the Flutter SDK, which the pure-Dart workspace does not use)
# and is analyzed from inside app/ with `flutter analyze`.
analyzer:
  exclude:
    - app/**
    # Build output, which the workspace must not analyze.
    - build/**
